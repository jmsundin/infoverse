{
  "name": "infoverse-monorepo",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "build": "cd client && npm install && npm run build"
  },
  "dependencies": {
    "@google/genai": "^1.31.0",
    "express": "^4.18.2",
    "pg": "^8.16.3"
  }
}
